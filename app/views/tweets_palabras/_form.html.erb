<%= form_for(@tweets_palabra) do |f| %>
  <% if @tweets_palabra.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tweets_palabra.errors.count, "error") %> prohibited this tweets_palabra from being saved:</h2>

      <ul>
      <% @tweets_palabra.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :id_tweet %><br>
    <%= f.text_area :id_tweet %>
  </div>
  <div class="field">
    <%= f.label :id_palabra %><br>
    <%= f.text_area :id_palabra %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
