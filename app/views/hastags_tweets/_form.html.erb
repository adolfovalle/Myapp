<%= form_for(@hastags_tweet) do |f| %>
  <% if @hastags_tweet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@hastags_tweet.errors.count, "error") %> prohibited this hastags_tweet from being saved:</h2>

      <ul>
      <% @hastags_tweet.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
